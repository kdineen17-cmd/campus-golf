// Bumped by hand on every meaningful publish so we can visually confirm
// which bundle is actually running on a device, instead of guessing
// whether an EAS Update cache is stale.
export const BUILD_MARKER = "2026-07-23.1";
