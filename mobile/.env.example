# Base URL of the backend API. On a physical device, "localhost" refers to
# the phone itself, so point this at your computer's LAN IP, e.g.
# http://192.168.1.42:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
